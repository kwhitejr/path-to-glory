# Environment variables for local development
# Copy this file to .env.local and fill in the values

# Application URL (use localhost for development)
VITE_APP_URL=http://localhost:5173

# GraphQL API Endpoint
# Local development
VITE_GRAPHQL_ENDPOINT=http://localhost:4000/graphql
# Production (set in GitHub Actions)
# VITE_GRAPHQL_ENDPOINT=https://api.kwhitejr.com/graphql

# AWS Cognito Configuration
# Get these values by running: cd infrastructure/frontend && terraform output

# Cognito User Pool ID (format: us-east-1_XXXXXXXXX)
VITE_COGNITO_USER_POOL_ID=

# Cognito User Pool Client ID
VITE_COGNITO_USER_POOL_CLIENT_ID=

# Cognito Identity Pool ID (format: us-east-1:xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx)
VITE_COGNITO_IDENTITY_POOL_ID=

# Cognito Domain (format: your-domain.auth.us-east-1.amazoncognito.com)
VITE_COGNITO_DOMAIN=
